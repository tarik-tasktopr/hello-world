Uses the DOMContentLoaded event listener to wait for the HTML document to load before executing the code.
Selects the element with the ID "hello-world" using querySelector.
Uses setInterval to repeatedly execute a function every 50 milliseconds.
The function updates the hue value and sets the color of the "hello-world" element using HSL color format.